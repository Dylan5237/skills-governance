---
name: agent-project-ops
alwaysApply: true
---

Read root `AGENTS.md` and follow the pinned `.agent-project-ops/PRINCIPLES.md`. GitHub `origin` is the single write authority; unknown remotes fail closed; use `.worktrees/`; PR merge is not Phase PASS. Colleague GitLab is share-export, not a full-tree projection.
